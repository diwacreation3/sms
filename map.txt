******* Variable *********
int class;
    char name[MAX], parent_name[MAX], addr[MAX];
    long long int phone;
    char dob[MAX];

********** file scan *******
fprintf(info, "%s\t  %s\t %s \t %s \t %lld\t\n", name, dob, addr, parent_name, phone);

fscanf(info_read, "%s %s %s  %s  %lld", name, dob, &addr, parent_name, &phone) != EOF